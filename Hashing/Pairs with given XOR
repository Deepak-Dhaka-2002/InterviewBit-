int Solution::solve(vector<int> &A, int B) {
    int result = 0;
    unordered_map<int,int> m;
    
    for(int i=0; i<A.size(); i++){
      if(m.find(B^A[i]) != m.end())
         result = result + m[B^A[i]];   
    m[A[i]]++; 
    }
    return result;
}
